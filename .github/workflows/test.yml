name: Test

on:
  push:

jobs:
  analyze:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v2

    - name: Print Warning Message
      run: echo "::warning file=app.js,line=1,col=5::View commit metadata at ${{ secrets.ARTIFACT_ROOT_URL }}/${{github.sha}}/test.html"
